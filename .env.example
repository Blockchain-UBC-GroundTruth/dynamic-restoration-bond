# Used by the seed/status scripts. The frontend reads public/demo-config.json.
GROUNDTRUTH_RPC_URL=https://api.devnet.solana.com

# Optional delay between Devnet seed transactions to avoid public-RPC 429s.
GROUNDTRUTH_RPC_PAUSE_MS=2500
